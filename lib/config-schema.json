{
  "lintOnChange": {
    "type": "boolean",
    "default": false,
    "title": "Lint on File Change",
    "description": "Lints the file on any change. Otherwise only on save."
  },
  "startServer": {
    "type": "boolean",
    "default": false,
    "title": "Start PL/SQL Lint Server",
    "description": "Starts the PL/SQL Lint Server if it is not running."
  },
  "port": {
    "type": "integer",
    "default": 9876,
    "title": "Port",
    "description": "Port to use when starting the PL/SQL Lint Server."
  },
  "plsqlLintServerSrc": {
    "type": "string",
    "default": "/",
    "title": "PL/SQL Lint Server Location",
    "description": "Location of the PL/SQL Lint server install. The file should contain a bin and lib folder."
  }
}
